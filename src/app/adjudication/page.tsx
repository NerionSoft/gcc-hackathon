import { WarRoom } from "@/presentation/features/war-room/war-room";

export const dynamic = "force-dynamic";

/** F4 — the adjudication war room. */
export default function AdjudicationPage() {
  return <WarRoom />;
}
